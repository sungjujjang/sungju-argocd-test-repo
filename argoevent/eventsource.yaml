apiVersion: argoproj.io/v1alpha1
kind: EventSource
metadata:
  name: webhook
  namespace: argo-events
spec:
  webhook:
    incoming:
      endpoint: /message
      jsonBody: true
      method: POST
      port: "12000"
