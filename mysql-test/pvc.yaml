apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-test-data
  namespace: mysql-test
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 10Gi
