apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: yakle-pvc
  namespace: yakle
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 2Gi